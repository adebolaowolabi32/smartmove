<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow
  http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">

    <view-state id="book" view="tickets/book" model="flowScope.scheduleBooking">
        <on-entry>
            <set name="flowScope.scheduleBooking"
                 value="new com.interswitch.smartmoveserver.model.view.ScheduleBooking()"/>
          <!--  <evaluate result="viewScope.today" expression=
                    "T(com.interswitch.smartmoveserver.util.DateUtil).getTodayDate())"/>-->
            <evaluate result="viewScope.terminals" expression="ticketService.getTerminals()"/>
            <evaluate result="viewScope.schedules" expression="ticketService.getSchedules()"/>
        </on-entry>
        <transition on="findBooking" to="schedule">
            <evaluate result="flowScope.scheduleBooking" expression=
                    "ticketService.findBooking(currentUser.name, flowScope.scheduleBooking)"/>
        </transition>
        <on-exit>
            <evaluate expression="T(org.apache.commons.logging.LogFactory).getLog(getClass()).info('Out now')"/>
        </on-exit>
    </view-state>

    <view-state id="schedule" view="tickets/schedule" model="flowScope.scheduleBooking">
        <on-entry>
            <evaluate expression="T(org.apache.commons.logging.LogFactory).getLog(getClass()).info('In now')"/>
        </on-entry>
        <transition on="makeBooking" to="details">
            <evaluate result="flowScope.ticketDetails" expression=
                    "ticketService.makeBooking(requestParameters.scheduleId, requestParameters.noOfPassengers)"/>
        </transition>
    </view-state>

    <view-state id="details" view="tickets/details" model="flowScope.ticketDetails">
        <transition on="next" to="confirmation">
            <evaluate result="flowScope.ticketDetails" expression=
                    "ticketService.getTickets(currentUser.name, flowScope.ticketDetails)"/>
        </transition>
        <transition on="back" to="schedule"/>
    </view-state>

    <view-state id="confirmation" view="tickets/confirmation" model="flowScope.ticketDetails">
        <transition on="confirmBooking" to="preview">
            <evaluate result="flowScope.ticketDetails" expression=
                    "ticketService.confirmTickets(currentUser.name, flowScope.ticketDetails)"/>
        </transition>
        <transition on="back" to="details"/>
    </view-state>

    <view-state id="preview" view="tickets/preview" model="flowScope.ticketDetails">
    </view-state>

    <end-state id="cancel"/>
</flow>